name: Container Image CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: anchore/scan-action@v3
        with:
          image: "alpine/git:latest"
          fail-build: true
